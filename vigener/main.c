#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main(const int argc, char** argv)
{
	//use guide
	if(argc < 2)
	{
		printf("./main -f source.txt\n");
		exit(0);
	}

	//get file name
	

	return 0;
}